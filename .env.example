# ============================================================================
# ENCRYPTION KEY (Generate with: python generate_key.py)
# ============================================================================
# Keep this secret! Store in Azure Key Vault for production
ENCRYPTION_KEY=your_generated_encryption_key_here

# ============================================================================
# ENCRYPTED SENSITIVE VALUES (Encrypt with: python encrypt_value.py)
# ============================================================================
# Prefix encrypted values with ENC: - they will be auto-decrypted by config.py

# Database (Supabase)
SUPABASE_URL=your_supabase_url
SUPABASE_KEY=ENC:your_encrypted_supabase_key_here

# Email (SendGrid)
SENDGRID_API_KEY=ENC:your_encrypted_sendgrid_key_here
SENDGRID_FROM_EMAIL=your_from_address@example.com

# SMS (Twilio)
TWILIO_ACCOUNT_SID=ENC:your_encrypted_twilio_sid_here
TWILIO_AUTH_TOKEN=ENC:your_encrypted_twilio_token_here
TWILIO_PHONE_NUMBER=+919182233429

# Webhook Security
WEBHOOK_SECRET=ENC:your_encrypted_webhook_secret_here

# ============================================================================
# CELERY + REDIS CONFIGURATION
# ============================================================================
# Localhost
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# Production (Azure Redis Cache - use rediss:// for SSL)
# CELERY_BROKER_URL=rediss://:password@your-redis.redis.cache.windows.net:6380/0
# CELERY_RESULT_BACKEND=rediss://:password@your-redis.redis.cache.windows.net:6380/0

# Worker Configuration
CELERY_WORKER_CONCURRENCY=20
CELERY_WORKER_PREFETCH_MULTIPLIER=4

# ============================================================================
# NOTES
# ============================================================================
# 1. Generate encryption key: python generate_key.py
# 2. Encrypt values: python encrypt_value.py
# 3. Values with ENC: prefix are automatically decrypted by config.py
# 4. Plain values (without ENC:) work as before (backward compatible)
# 5. Never commit ENCRYPTION_KEY to Git!
